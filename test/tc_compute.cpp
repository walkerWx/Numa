#include <iRRAM.h>
#include <string>

using namespace std;
using namespace iRRAM;
void compute() { 
  const REAL c,d;
  static REAL iab,ss;
}

int compute(int i, int j) {
  return 1;
}

void compute(int i) {

}
static std::string foo() {
  return "foo";
}

int *i;

REAL realFunc(){
  REAL x = 1,y;
  return x;
}

int f(){
  REAL z;
  for(int i = 0; i < 10; i++){};
  int a = 5;
  double b = 4;
  a = a + b;
  if( a == b ){
  
  } else {
    
  }
  return 0;
}

class A {
public:
  REAL a(){
    REAL x = 1;
    return x;
  }
};
